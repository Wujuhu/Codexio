---
description: 每次改完代码后必须重新打包 EXE
alwaysApply: true
---

# 改完即打包

用户修改请求完成后，在收尾前必须重新打包 Windows EXE，不要等用户再提醒。

- 用项目根目录的 `.\build_exe.ps1` 打包。
- 若 `dist\Codexio.exe` 被占用，保留原文件与暂存新版，提示用户退出后重试；不得自动结束用户进程，也不要在 `dist` 创建副本。
- 回复里写明生成的 EXE 路径。
- 纯问答、只读排查、没有改仓库文件时不打包。
