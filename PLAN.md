# Codexio 本地源码版开发计划

## 1. 项目目标

开发一个仅供本机运行的 Windows 桌面小组件，实时显示当前 Codex 账户的额度：

- 5 小时额度
- 1 周额度
- 剩余百分比
- 预计重置时间
- 最近一次成功更新时间

本阶段只开发源码运行版本，不制作安装包、不做自动更新、不做跨平台发布。

## 2. 目标平台与技术选型

- 操作系统：Windows 10/11，优先支持 x64。
- 运行环境：Python 3.11+。
- GUI：PySide6。
- 通信方式：启动一个长驻的 `codex app-server`，通过 JSONL/JSON-RPC 通信。
- Codex 可执行文件：优先从 PATH 查找，其次检查本机 Codex 默认安装位置。

## 3. 额度获取方案

使用本机已登录的 Codex，通过以下请求读取额度：

```text
account/rateLimits/read
```

程序不执行模型请求，不抓取 ChatGPT 网页，不直接读取或保存 `auth.json` 中的凭据。

刷新策略：

- 默认每 60 秒读取一次。
- 可选间隔：15 秒、30 秒、60 秒、5 分钟。
- 收到 `account/rateLimits/updated` 通知后立即更新。
- 请求失败时采用退避重试，最长间隔 5 分钟。

## 4. 功能范围

### 4.1 额度显示

- 显示 5 小时和 1 周剩余百分比。
- 显示对应重置时间。
- 显示“正在读取”“读取失败”“数据过期”等状态。
- 对缺失的额度窗口显示 `N/A`，不伪造数据。

### 4.2 三种显示模式

1. **底层模式**：普通窗口，不置顶，尽量保持在普通窗口下方。
2. **顶层模式**：始终置顶。
3. **托盘模式**：隐藏主窗口，只显示系统托盘图标，点击后显示/隐藏窗口。

托盘模式暂时采用 Windows 通知区域图标；真正的任务栏按钮后续再扩展。

### 4.3 基础交互

- 手动刷新。
- 切换显示模式。
- 设置刷新间隔。
- 拖动窗口。
- 记住窗口位置和用户设置。
- 退出时正确结束后台 `codex app-server`。

## 5. 建议目录结构

```text
Codexio/
├─ PLAN.md
├─ README.md
├─ requirements.txt
├─ run.ps1
├─ src/
│  └─ codexio/
│     ├─ __main__.py
│     ├─ app_server.py       # app-server 启动、JSON-RPC 通信
│     ├─ rate_limits.py      # 额度模型和窗口转换
│     ├─ worker.py           # 后台读取、通知、重试和缓存
│     ├─ settings.py         # 配置读写
│     ├─ tray.py             # 系统托盘
│     ├─ window.py           # 悬浮窗和三种显示模式
│     └─ logging_setup.py    # 日志配置
└─ tests/
   ├─ fixtures/              # JSON-RPC 样例数据
   ├─ test_rate_limits.py
   └─ test_app_server_parser.py
```

## 6. 开发阶段

### 阶段一：项目骨架

- 创建 Python 包、依赖文件和启动脚本。
- 加入模拟 JSON-RPC 数据。
- 先完成额度解析单元测试。

### 阶段二：Codex app-server 客户端

- 自动发现 `codex.exe`/`codex.cmd`。
- 启动长驻子进程并完成 `initialize` 握手。
- 实现 `account/rateLimits/read` 请求。
- 处理标准输出中的响应和异步通知。
- 处理 Codex 进程退出、超时和无效 JSON。

### 阶段三：额度服务层

- 将 300 分钟窗口映射为“5 小时”。
- 将 10080 分钟窗口映射为“1 周”。
- 计算剩余百分比。
- 添加内存缓存、最后成功时间和错误状态。
- 实现定时刷新、通知刷新和退避重试。

### 阶段四：桌面界面

- 完成无边框悬浮窗。
- 实现底层、顶层和托盘三种模式。
- 显示额度、重置时间和状态颜色。
- 加入右键菜单和手动刷新。

### 阶段五：配置与日志

- 保存刷新间隔、窗口位置、显示模式和 Codex 路径。
- 配置和缓存放在 `%LOCALAPPDATA%\\Codexio`。
- 日志中禁止写入访问令牌或敏感认证内容。

### 阶段六：本机联调

- 使用当前 Windows 账户实际运行 Codex app-server。
- 验证 5 小时/1 周额度显示。
- 验证刷新、通知、进程重启和退出清理。
- 验证三种窗口模式。
- 编写源码运行说明和故障排查说明。

## 7. MVP 验收标准

- 双击或执行脚本即可启动源码版本。
- 能发现本机 Codex 并读取真实额度。
- 5 小时和 1 周额度显示正确。
- 默认最多 60 秒完成一次兜底刷新。
- app-server 异常退出后能自动恢复或给出明确错误。
- 网络异常、登录失效时程序不崩溃。
- 三种显示模式可以正常切换。
- 退出程序后没有残留后台 Codex 进程。

## 8. 暂不处理的内容

- 安装包、代码签名和自动更新。
- macOS/Linux 支持。
- 多账户切换。
- 直接调用非公开 `wham/usage` 接口。
- 读取本地日志推算官方额度。
- 云端同步、遥测和账号管理。

## 9. 下一步

先执行阶段一：创建项目骨架、依赖文件和额度解析测试；随后再接入真实的 `codex app-server`。
